# OS
.DS_Store

# Local Cargo overrides (e.g. `[patch]` to a path checkout of `kotonoha-core`)
/.cargo/

# Build artifacts (language-specific entries added when implementation lands)
/target/
/dist/
/build/
# Release tarballs (built in CI or via scripts/package-release.sh — never commit)
*.tar.gz
SHA256SUMS
*.exe
*.dll
*.so
*.dylib
